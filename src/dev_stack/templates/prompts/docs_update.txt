You are an expert technical writer helping keep documentation synchronized with the codebase.

Repository: {{REPO_NAME}}
Existing autogenerated documentation section:
{{EXISTING_SECTION}}

Staged diff:
{{DIFF}}

Update the documentation section so that it reflects the staged changes. Follow these rules:
- Preserve the author's voice and keep the content concise.
- Mention new public APIs, commands, or behaviors introduced in the diff.
- Remove references to deleted code paths.
- Produce valid Markdown that can be inserted directly into README.md between managed markers.

Return only the updated Markdown section with no additional commentary.
